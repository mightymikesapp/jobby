# Maintenance

Review `get_source_health`, `list_pending_reviews`, and `list_alerts`. Run
backups and recovery checks through the explicit CLI maintenance commands.
Investigate partial scans, stale postings, duplicate groups, and failed source
circuits before relying on the shortlist. Keep external catalog credentials in
the OS keyring and out of logs, prompts, exports, and backups.
