# Offer comparison

Use `list_offers` or `compare_offers` to show base, bonus, annualized equity,
cost-of-living adjustment, stress adjustment, and terms. Explain that the
calculation is a decision aid, not financial advice. Record a user-selected
decision with `set_offer_decision`; never infer acceptance or rejection.
