# Interview preparation and review

Use `list_questions` and role tags to select a bounded preparation set. Build a
session with `create_interview_session`, grounding answers in approved facts or
explicit evidence references. Afterward record the retrospective and any
follow-up task with `record_interview_review`.
